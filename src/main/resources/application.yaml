spring:
  datasource:
    url: jdbc:postgresql://tcp-mo5.mogenius.io:41398/covid-db
    username: postgres
    password: sudo
  jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      show-sql : true
      hibernate.ddl-auto: create-drop