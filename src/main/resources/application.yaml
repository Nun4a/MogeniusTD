server:
  port: 9797
spring:
  datasource:
    url: jdbc:postgresql://database-f7qztf:5432/covid-db
    username: posgres-user
    password: sudo
  jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      show-sql : true
      hibernate.ddl-auto: create-drop