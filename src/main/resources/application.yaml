spring:
  datasource:
    url: jdbc:postgresql://dbapi-kcslp0:5432/covid-db
  liquibase:
    enabled: false
  jpa:
      database-platform: org.hibernate.dialect.PostgreSQLDialect
      show-sql : true
      hibernate.ddl-auto: create-drop